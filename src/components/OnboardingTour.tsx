import { useState } from 'react';
import { ArrowRight, Flame, Sparkles, Star } from 'lucide-react';
const steps=[['Stories','Partagez des instants qui disparaissent après 24 h.',Sparkles],['Pop Sessions','Rejoignez des discussions live avec votre communauté.',Flame],['Aura','Votre Aura reflète votre activité et vos interactions.',Star],['Axo Coins','Gagnez des coins avec les activités proposées.',Sparkles]] as const;
export default function OnboardingTour({onFinish,isDark}:{onFinish:()=>void;isDark:boolean}){const [index,setIndex]=useState(0);const [title,text,Icon]=steps[index];const next=()=>index===steps.length-1?onFinish():setIndex(index+1);return <div role="dialog" aria-modal="true" aria-labelledby="onboarding-title" className="fixed inset-0 z-[300] flex items-center justify-center bg-[var(--axo-overlay)] p-6 text-center text-[var(--axo-text)] backdrop-blur-md"><div className="w-full max-w-sm rounded-[30px] border border-[var(--axo-border)] bg-[var(--axo-surface-strong)] p-7 shadow-2xl"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-[#FF2D55]/10"><Icon className="h-8 w-8 text-[#FF2D55]"/></div><p className="mt-8 text-[10px] font-black tracking-widest text-[#FF2D55]">BIENVENUE SUR AXORA · {index+1}/4</p><h1 id="onboarding-title" className="mt-3 text-3xl font-black">{title}</h1><p className="mt-3 text-sm leading-relaxed text-[var(--axo-text-muted)]">{text}</p><div className="mt-8 grid grid-cols-4 gap-2" aria-label={`Étape ${index+1} sur ${steps.length}`}>{steps.map((_,i)=><span key={i} className={`h-1 rounded-full ${i<=index?'bg-[#FF2D55]':isDark?'bg-zinc-800':'bg-zinc-200'}`}/>)}</div><button type="button" onClick={next} className="mt-8 flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-[#FF2D55] py-3 text-xs font-black text-white focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#FF2D55]">{index===3?'Entrer sur Axora':'Continuer'}<ArrowRight className="h-4 w-4"/></button><button type="button" onClick={onFinish} className="mt-4 min-h-11 text-[10px] font-bold text-[var(--axo-text-muted)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#FF2D55]">Passer le tutoriel</button></div></div>}
