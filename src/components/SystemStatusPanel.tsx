import { useState } from 'react';
import { AlertTriangle, RefreshCw, Wrench, X } from 'lucide-react';
export default function SystemStatusPanel(){const [mode,setMode]=useState<'maintenance'|'update'|'error'|null>(null);return <section className="mt-4 rounded-2xl border border-[var(--axo-border)] bg-[var(--axo-surface)] p-4"><h3 className="text-xs font-black">État du service</h3><p className="mt-1 text-[10px] text-zinc-500">Simuler les écrans système du front-end.</p><div className="mt-3 grid grid-cols-3 gap-2">{([['maintenance','Maintenance'],['update','Mise à jour'],['error','Erreur']] as const).map(([id,label])=><button key={id} type="button" onClick={()=>setMode(id)} className="rounded-xl border border-[var(--axo-border)] p-2 text-[9px] font-bold">{label}</button>)}</div>{mode&&<div className="fixed inset-0 z-[200] flex items-center justify-center bg-zinc-950 p-5 text-center text-white"><button type="button" onClick={()=>setMode(null)} className="absolute right-5 top-5"><X/></button><div className="max-w-sm"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-[#FF2D55]/10">{mode==='maintenance'?<Wrench className="text-[#FF2D55]"/>:mode==='update'?<RefreshCw className="text-cyan-400"/>:<AlertTriangle className="text-amber-400"/>}</div><h2 className="mt-5 text-xl font-black">{mode==='maintenance'?'Maintenance en cours':mode==='update'?'Mise à jour requise':'Service indisponible'}</h2><p className="mt-3 text-xs leading-relaxed text-zinc-400">{mode==='maintenance'?'Axora revient bientôt. Vos données locales restent protégées.':mode==='update'?'Une nouvelle version est nécessaire pour continuer.':'Une erreur serveur simulée est survenue. Réessayez dans quelques instants.'}</p><button type="button" onClick={()=>setMode(null)} className="mt-6 rounded-xl bg-[#FF2D55] px-5 py-3 text-[10px] font-black">{mode==='update'?'Mettre à jour':'Réessayer'}</button></div></div>}</section>}
