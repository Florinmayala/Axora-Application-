import { useState } from 'react';
import { Bell } from 'lucide-react';
export default function NotificationPreferences() { const [types,setTypes]=useState({social:true,messages:true,pop:true,security:true,coins:false}); const entries=[['social','Interactions'],['messages','Messages'],['pop','Pop Sessions'],['security','Sécurité'],['coins','Axo Coins']] as const; const active=entries.find(([key])=>types[key]); return <section className="mt-4 rounded-2xl border border-[var(--axo-border)] bg-[var(--axo-surface)] p-4"><h3 className="flex items-center gap-2 text-xs font-black"><Bell className="h-4 w-4 text-[#FF2D55]" />Préférences de notifications</h3><div className="mt-3 space-y-2">{entries.map(([key,label])=><button key={key} type="button" onClick={()=>setTypes(current=>({...current,[key]:!current[key]}))} className="flex w-full items-center justify-between rounded-xl border border-[var(--axo-border)] px-3 py-2 text-[10px] font-bold"><span>{label}</span><span className={`h-5 w-9 rounded-full p-0.5 ${types[key]?'bg-[#FF2D55]':'bg-zinc-600'}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${types[key]?'translate-x-4':''}`}/></span></button>)}</div><div className="mt-3 rounded-xl bg-[#FF2D55]/5 p-3 text-[10px]"><b className="text-[#FF2D55]">APERÇU</b><p className="mt-1 text-zinc-500">{active ? `${active[1]} : nouvelle alerte Axora` : 'Aucune alerte activée.'}</p></div></section>; }
