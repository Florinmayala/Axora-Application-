import { useState } from 'react';
import { Download, ShieldCheck, UserRoundCheck } from 'lucide-react';
export default function AdvancedAccountPanel(){const [verified,setVerified]=useState(false);const [parental,setParental]=useState(false);const [exported,setExported]=useState(false);const exportData=()=>{const blob=new Blob([JSON.stringify({profile:localStorage.getItem('axo_profileName'),exportedAt:new Date().toISOString()},null,2)],{type:'application/json'});const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download='axora-data.json';link.click();URL.revokeObjectURL(link.href);setExported(true)};return <section className="mt-4 rounded-2xl border border-[var(--axo-border)] bg-[var(--axo-surface)] p-4 text-left"><h3 className="text-xs font-black">Compte avancé</h3><div className="mt-3 rounded-xl border border-[var(--axo-border)] p-3"><div className="flex items-center gap-2 text-[10px] font-black"><ShieldCheck className="h-4 w-4 text-emerald-500"/>Statut de vérification</div><p className="mt-1 text-[10px] text-zinc-500">{verified?'Compte vérifié dans cette maquette.':'Votre identité n’est pas encore vérifiée.'}</p><button type="button" onClick={()=>setVerified(true)} disabled={verified} className="mt-2 rounded-lg bg-emerald-500/10 px-3 py-2 text-[10px] font-bold text-emerald-500 disabled:opacity-50">{verified?'Vérifié':'Simuler la vérification'}</button></div><div className="mt-3 rounded-xl border border-[var(--axo-border)] p-3"><div className="flex items-center justify-between gap-3"><span className="flex items-center gap-2 text-[10px] font-black"><UserRoundCheck className="h-4 w-4 text-amber-500"/>Contrôle parental / âge</span><button type="button" onClick={()=>setParental(v=>!v)} className={`h-5 w-9 rounded-full p-0.5 ${parental?'bg-[#FF2D55]':'bg-zinc-600'}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${parental?'translate-x-4':''}`}/></button></div><p className="mt-1 text-[10px] text-zinc-500">{parental?'Mode protégé actif : contenus sensibles limités.':'Mode protégé inactif.'}</p></div><button type="button" onClick={exportData} className="mt-3 flex w-full items-center justify-center gap-2 rounded-xl border border-cyan-400/30 bg-cyan-400/5 py-2.5 text-[10px] font-black text-cyan-400"><Download className="h-3.5 w-3.5"/>{exported?'Export téléchargé':'Exporter mes données'}</button></section>}
