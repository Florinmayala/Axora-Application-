import { useState } from 'react';
import { ShieldAlert } from 'lucide-react';
export default function ModerationPanel(){const [reports,setReports]=useState([{id:'R-103',text:'Spam dans Pop Design',status:'En attente'},{id:'R-102',text:'Message signalé',status:'Traité'}]);const [members,setMembers]=useState(['Lena X — admin','Kaelen — membre']);return <section className="mt-4 rounded-2xl border border-[var(--axo-border)] bg-[var(--axo-surface)] p-4"><h3 className="flex items-center gap-2 text-xs font-black"><ShieldAlert className="h-4 w-4 text-red-500"/>Modération communautaire</h3><p className="mt-3 text-[10px] font-black">Signalements envoyés</p>{reports.map(report=><div key={report.id} className="mt-2 rounded-xl border border-[var(--axo-border)] p-3 text-[10px]"><b>{report.id} · {report.status}</b><p className="mt-1 text-zinc-500">{report.text}</p><button onClick={()=>setReports(current=>current.map(item=>item.id===report.id?{...item,status:'Traité — réponse envoyée'}:item))} className="mt-2 text-cyan-400">Répondre / traiter</button></div>)}<p className="mt-4 text-[10px] font-black">Membres du groupe / Pop</p>{members.map(member=><div key={member} className="mt-2 flex items-center justify-between rounded-xl border border-[var(--axo-border)] p-3 text-[10px]"><span>{member}</span><button onClick={()=>setMembers(current=>current.filter(item=>item!==member))} className="text-red-500">Exclure</button></div>)}<p className="mt-3 text-[10px] text-zinc-500">Messages supprimés : 0 · Rôles : admin et membre.</p></section>}
